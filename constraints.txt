# Versiones fijas para reproducibilidad (ver CLAUDE.md, seccion "Reproducibilidad").
# El hash del dataset generado solo esta garantizado con estas versiones.
#
# Nota: pyspark 3.5.x no soporta oficialmente Python 3.13; la CI usa Python 3.11.
# pyspark 3.5.3 aun importa `distutils`, que Python 3.12 elimino de la libreria estandar.
# setuptools reinstala el shim (`distutils-precedence.pth`), asi que en un entorno con
# Python >= 3.12 es obligatorio: sin el, todo test que toque Spark falla al importar.
setuptools==84.0.0

numpy==2.2.0
pandas==2.2.3
Faker==33.1.0
pyspark==3.5.3
py4j==0.10.9.7
scikit-learn==1.6.0
lightgbm==4.5.0
scipy==1.14.1
joblib==1.4.2
threadpoolctl==3.5.0
python-dateutil==2.9.0.post0
pytz==2024.2
tzdata==2024.2
pytest==8.3.4

# --- Fase 2: ETL, EDA y notebook ---
pyarrow==18.1.0
matplotlib==3.10.0
seaborn==0.13.2
nbformat==5.10.4
nbclient==0.11.0
nbconvert==7.17.1
ipykernel==6.29.5
jupyter_client==8.6.3
jupyter_core==5.7.2
traitlets==5.14.3

# --- Fase 5: registro de experimentos (opcional, no esta en requirements.txt) ---
mlflow==3.15.2

# --- Fase 6a: preparacion visual del catalogo ---
requests==2.32.3
python-dotenv==1.2.2
pillow==11.0.0
certifi==2024.8.30
urllib3==2.2.3
charset-normalizer==3.4.0
idna==3.10

# --- Fase 6b: demo web interactiva ---
# streamlit arrastra bastante cadena propia (altair para los graficos, pydeck, el servidor
# uvicorn/starlette). Se fijan aqui las que traen binarios o rompen entre versiones
# menores; las de solo texto se dejan resolver a pip.
streamlit==1.63.0
altair==6.2.2
narwhals==2.25.0
protobuf==7.36.1
pydeck==0.9.3
watchdog==6.0.0
uvicorn==0.52.4
starlette==1.6.0
websockets==16.1.1
jsonschema==4.26.0
Jinja2==3.1.6
click==8.5.0
toml==0.10.2
